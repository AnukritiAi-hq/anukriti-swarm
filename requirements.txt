# Anukriti Swarm - Python Dependencies
# Install: pip install -r requirements.txt

# Core
pydantic==2.7.1
fastapi==0.111.0
uvicorn==0.29.0
websockets==15.0.1

# AI/ML
openai==1.30.1
anthropic==0.25.8

# Data
pandas==2.2.2
numpy==1.26.4

# MCP persistence (pymongo is now a first-class dependency; the MCP
# layer still falls back to in-memory when MONGODB_URI is not set)
pymongo==4.17.0

# Dev
pytest==8.2.0
ruff==0.4.4
mypy==1.10.0

# Deterministic pharmacogenomics core.
# 13-gene CPIC-pinned phenotype engine + VCF callers + provenance manifest.
# rules/phenotype_rules.py is a thin re-export over this package.
# Published to PyPI from https://github.com/AnukritiAi-hq/anukriti-pgx-core
#
# Pin the patch version exactly — the library is the biomedical truth
# layer and new versions may contain CPIC table updates that require
# regression review in Swarm before adoption. Bump this pin deliberately.
anukriti-pgx-core==0.2.1

# -----------------------------------------------------------------------------
# Optional: Agents Assemble hackathon (Prompt Opinion MCP Superpower)
# -----------------------------------------------------------------------------
# Only required if you are running the hackathon/ MCP server. These deps
# are not imported anywhere in the main swarm (agents/, core/, retrieval/,
# verification/, etc.). See hackathon/README.md for details.
#
# To install: pip install -r hackathon/requirements.txt
#
# fastmcp>=3.2.4           # Prompt Opinion reference MCP stack
# fhir.resources>=8.2.0    # Typed FHIR R4 resources
# httpx>=0.28.1            # Async HTTP for FhirClient
